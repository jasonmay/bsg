{
  "id": "bsg-a048",
  "name": "use embedded files instead of large string literals",
  "type": "constraint",
  "status": "draft",
  "body": "Large text content (templates, README content, etc.) must use go:embed with external files rather than string literals or raw string constants. This keeps the Go source readable, makes the content editable with proper syntax highlighting, and avoids backtick escaping issues.",
  "tags": [
    "code-quality",
    "go"
  ]
}
